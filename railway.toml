[build]
buildCommand = "cd judicary_backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd judicary_backend && gunicorn app:app --bind 0.0.0.0:${PORT:-5000} --workers 2 --timeout 120"
